import React from 'react';

import {Pressable, ScrollView, Text} from 'react-native';

export function Account() {
  return (
    <ScrollView>
      <Pressable>
        <Text>Logout</Text>
      </Pressable>
    </ScrollView>
  );
}
